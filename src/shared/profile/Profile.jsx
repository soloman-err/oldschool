import React from 'react';
import { Helmet } from 'react-helmet-async';

const Profile = () => {
    return (
        <section>
            <Helmet><title>oldschool | profile</title></Helmet>
            User
        </section>
    );
};

export default Profile;